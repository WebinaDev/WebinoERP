/**
 * @deprecated Site Builder is folded into the Platform module.
 * Do not register this file in `kernel/registry.ts`.
 * Admin pages live under `admin/platform/sites` and `admin/platform/catalog`.
 */
export const moduleSlug = "platform"
