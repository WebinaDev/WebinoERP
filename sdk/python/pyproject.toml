[project]
name = "webina-erp-sdk"
version = "1.0.0"
description = "Python client for WebinoERP API"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
